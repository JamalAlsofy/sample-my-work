import { createActionGroup, emptyProps, props } from '@ngrx/store';

export const CriticalStateActions = createActionGroup({
  source: 'CriticalState',
  events: {
    'Caritical CriticalStates': emptyProps(),
    
    
  }
});
