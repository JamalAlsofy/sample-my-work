import { Route } from '@angular/router';
import { ProductTableComponent } from './product-table.component';

export const productTableRoutes: Route[] = [{ path: '', component: ProductTableComponent }];
