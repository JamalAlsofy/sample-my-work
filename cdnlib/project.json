{
  "name": "cdnlib",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "cdnlib/src",
  "prefix": "lib",
  "projectType": "library",
  "tags": [],
  "targets": {
    "test": {
      "executor": "@nx/vite:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "../coverage/cdnlib"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
